<!--saisai-->
<% if @status_edit %>
    <h4><%= _("Merge Articles") %></h4>
    <div class=''>
      <%= form_tag url_for(:controller => 'content', :action => 'merge_articles', :article_id => @article.id), :method => "update" do%>
          <%= label_tag(:article_id, "Article ID")%>
          <%= text_field_tag 'merge_with'%>
          <%= submit_tag("Merge") %>
      <% end %>
    </div>
<% end %>